# Day 12 - dependencies for the DEPLOYED simulation app (app.py)
#
# Streamlit Community Cloud installs this file. It is deliberately tiny:
# app.py runs the trained network with NumPy only, so TensorFlow is NOT
# needed to serve the app. See export_for_deploy.py for why.
#
# To TRAIN the model yourself (the practice scripts, the notebook and
# 4_ann_fashion_mnist.py) install requirements-train.txt instead.

streamlit>=1.40.0
numpy>=1.26.0
