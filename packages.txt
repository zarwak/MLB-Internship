# Streamlit Cloud reads packages.txt for `apt-get install` ONLY from the
# true repository root - same lesson as runtime.txt (see runtime.txt in
# this same folder): copies inside Day23/ and Day23/mini_project/ are
# NOT enough on their own, even though requirements.txt IS read from the
# app's own folder. Kept there too since it's harmless, but this root
# copy is the one that actually gets picked up for the Day23 OCR app.
#
# tesseract-ocr: the Tesseract OCR engine binary - pytesseract is only a
# Python wrapper around it, and pip cannot install a native binary.
#
# libgl1 / libglib2.0-0 / libsm6 / libxext6 / libxrender1 / libgomp1:
# easyocr, paddleocr and doctr all pull in opencv (cv2) transitively -
# none of Day23's own code imports it directly - and `import cv2` then
# fails with "libGL.so.1: cannot open shared object file" because this
# container has none of the system graphics libraries a non-headless
# opencv build expects.
tesseract-ocr
libgl1
libglib2.0-0
libsm6
libxext6
libxrender1
libgomp1
