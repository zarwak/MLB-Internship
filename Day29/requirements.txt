# Day 29 - Custom YOLO Object Detection (Road Damage Detection)

# The deployed app itself
streamlit>=1.40.0
ultralytics>=8.3.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
pillow>=10.0.0

# Video output as browser-playable H.264 mp4 (OpenCV's own build has no
# licensed H.264 encoder, so cv2.VideoWriter output won't play in a <video>
# tag - imageio's bundled ffmpeg wheel does the encoding instead).
imageio[ffmpeg]>=2.34.0

# Dataset download / prep tools (roboflow, python-dotenv, pyyaml) live in
# requirements-dev.txt, NOT here. app.py never imports them - it just loads
# the best.pt already committed to the repo. Streamlit Cloud installs
# whatever's in this file at deploy time regardless of what's actually
# imported, and roboflow's transitive deps (incl. the *non-headless*
# opencv-python) can get resolved to versions that are ABI-incompatible
# with opencv-python-headless/torch's precompiled wheels - which showed up
# in practice as a segfault on boot, not a clean ImportError. Keep this
# file minimal to what app.py/detection.py actually import.
